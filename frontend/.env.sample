# Local dev: point at the backend running on its own port.
# Production: leave this unset (or empty) — nginx serves the frontend and
# proxies /api/ to the backend under the same domain, so relative URLs work.
VITE_API_BASE_URL=http://localhost:8000
